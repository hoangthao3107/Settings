import MarketPage from "./MarketPage";

export default function App() {
  return (
    <div style={{ height: "100vh", overflow: "hidden", fontFamily: "'Inter', system-ui, sans-serif", color: "#343839" }}>
      <MarketPage />
    </div>
  );
}
